services:
  checkfeed:
    build: .
    container_name: checkfeed-bot
    restart: unless-stopped
    volumes:
      - ./data:/usr/src/app/data
      - ./config.json:/usr/src/app/config.json:ro
    environment:
      - TZ=Europe/Rome
      - PYTHONPATH=/usr/src/app
      - CHECKFEED_SECRET=${CHECKFEED_SECRET}
    networks:
      - checkfeed-net

networks:
  checkfeed-net:
    driver: bridge
